<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lhj.bookmanagementdemo.mapper.AdminMapper">
    <update id="updateById">
        update admin set username=#{username},phone=#{phone},email=#{email},updatetime=#{updatetime},status=#{status} where id=#{id}
    </update>
    <update id="updatePassword">
        update admin set password=#{newPass} where username=#{username} and password=#{password}
    </update>
    <delete id="deleteById">
        delete from admin where id=#{id}
    </delete>
    <select id="list" resultType="com.lhj.bookmanagementdemo.entity.Admin">
        select * from admin order by id asc
    </select>
    <select id="listByCondition" resultType="com.lhj.bookmanagementdemo.entity.Admin">
        select * from admin
        <where>
        <if test="username != null and username  != ''">
            and username like concat('%',#{username},'%')
        </if>
        <if test="phone != null and phone  != ''">
            and phone like concat('%',#{phone},'%')
        </if>
        <if test="email != null and email  != ''">
            and email like concat('%',#{email},'%')
        </if>
        </where>
        /*模糊查询，如果什么都不传，则查询所有*/
        order by id asc
    </select>
    <select id="save">
        insert into admin(username,phone,email,password) values(#{username},#{phone},#{email},#{password})
    </select>
    <select id="getById">
        select * from admin where id=#{id}
    </select>
    <select id="getByUsernameAndPassword" resultType="com.lhj.bookmanagementdemo.entity.Admin">
        select * from admin where username=#{username} and password=#{password}
    </select>
    <select id="getByUsername" resultType="com.lhj.bookmanagementdemo.entity.Admin">
        select * from admin where username= #{username}
    </select>
</mapper>